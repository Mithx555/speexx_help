# คู่มือทดสอบ

เอกสารนี้ใช้ตรวจ Speexx Helper ก่อนออกเวอร์ชันใหม่ โดยทดสอบกับบทเรียนที่บัญชีผู้ใช้มีสิทธิ์เข้าถึง

## ตรวจอัตโนมัติ

เปิด PowerShell ที่โฟลเดอร์โครงการ แล้วรัน:

```powershell
.\scripts\validate-extension.ps1
```

สคริปต์จะตรวจ manifest, ไฟล์หลัก, ไอคอน และไวยากรณ์ JavaScript หากมี Node.js ในเครื่อง

## รายการทดสอบบน Speexx Portal

ทำอย่างน้อยหนึ่งข้อของแต่ละชนิดที่พบ แล้วตรวจว่า `Correction`, `Repeat`, `Start`, การใส่คำตอบ และ `Next` ทำงานตามปกติ:

- Multiple choice / Single choice
- Fill text / Fill text toggle
- Drag & Drop
- Toggle solution
- Mark text
- Picture choice
- Scrambled sentence

## โหมดเวลาและการทำต่อเนื่อง

1. ตั้งเวลาต่อข้อและเวลาทบทวนเป็น 1 นาที
2. เริ่ม `ทำทั้งหมดแบบจับเวลา` แล้วตรวจตัวจับเวลามุมซ้ายล่างและ popup เตือน
3. เริ่ม `ทำอัตโนมัติต่อเนื่อง` แล้วกด `เรียนรู้ต่อ` เมื่อหน้าสรุปปรากฏ
4. ตรวจว่าหน้าถัดไปเริ่มทำต่อ และสรุปเซสชันยังคงค่าเดิมหลังหน้าโหลดใหม่
5. กด `↺` ในแถบสรุปตอนระบบหยุด แล้วตรวจว่าสถิติกลับเป็นศูนย์

## เมื่อพบปัญหา

กด `⧉ ส่งออก Debug` ในแผง Speexx Helper แล้วแนบข้อความรายงานพร้อมภาพหน้าจอและชนิดแบบฝึกหัดไว้ใน GitHub Issue

