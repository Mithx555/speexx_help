# คู่มือทดสอบ

ใช้รายการนี้ตรวจ Speexx Helper ก่อนปล่อยเวอร์ชันใหม่ โดยทดสอบกับบทเรียนที่บัญชีของคุณมีสิทธิ์เข้าใช้งาน

## เช็กพื้นฐานแบบอัตโนมัติ

เปิด PowerShell ในโฟลเดอร์โปรเจกต์ แล้วรัน:

```powershell
.\scripts\validate-extension.ps1
```

สคริปต์จะตรวจ manifest ไฟล์หลัก ไอคอน และไวยากรณ์ JavaScript หากเครื่องมี Node.js อยู่แล้ว

## ลองกับแบบฝึกหัดบน Speexx Portal

ทำแบบฝึกหัดอย่างน้อยหนึ่งข้อของแต่ละประเภทที่เจอ แล้วดูว่า `Correction`, `Repeat`, `Start`, การใส่คำตอบ และ `Next` ยังทำงานตามปกติ:

- Multiple choice / Single choice
- Fill text / Fill text toggle
- Drag & Drop
- Toggle solution
- Mark text
- Picture choice
- Scrambled sentence

## ลองโหมดเวลาและทำต่อเนื่อง

1. ตั้งเวลาต่อข้อและเวลาทบทวนเป็น 1 นาที
2. เริ่ม `ทำทั้งหมดแบบจับเวลา` แล้วดูตัวจับเวลาที่มุมซ้ายล่าง รวมถึง popup เตือนก่อนหมดเวลา
3. เริ่ม `ทำอัตโนมัติต่อเนื่อง` แล้วกด `เรียนรู้ต่อ` เมื่อหน้าสรุปขึ้น
4. ดูว่าหน้าถัดไปเริ่มทำต่อ และสรุปเซสชันยังอยู่ครบหลังหน้าเว็บโหลดใหม่
5. ตอนระบบหยุด ให้กด `↺` ในแถบสรุปและตรวจว่าสถิติกลับเป็นศูนย์

## ถ้าเจอปัญหา

กด `⧉ ส่งออก Debug` จากแผง Speexx Helper แล้วแนบรายงาน ภาพหน้าจอ และชื่อชนิดแบบฝึกหัดไว้ใน GitHub Issue ข้อมูลครบขึ้นก็ช่วยให้ตามแก้ได้เร็วขึ้น
